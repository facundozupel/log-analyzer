{
  "name": "log-analyzer-web",
  "version": "1.0.0",
  "description": "Web interface for log analyzer",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Static site - no build needed'"
  }
}
